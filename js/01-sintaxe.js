        // ctrl + ;       (comentario de linha unica)
        /* alt + shft + A       (comentarios de multiplas linhas) */
        
        
        /* Sempre que trabalhar com textos devemos colocar entre aspas simples '' ou aspas "" ou crase ¨  */


        // Comando de saída de dados
        console.log("Olá JavaScript!");

        /* Variaveis e constantes, espaços identificados na memória para armazenar dados temporariamente */

        // Variavel - let é igual a var (utilizado antigamente)
        let ano = 2022;

        // constante - valor não muda ou imutavel
        const aluno = "Vagner";

        console.log(ano);
        console.log(aluno);
        console.log(ano, aluno);

        /* Operações matemáticas
        + (adição)
        - (subtração)
        / (divisão)
        * (multiplicação) */

        let preco = 1500;
        let desconto = 250;
        let precoFinal = preco - desconto;

        console.log(preco, desconto, precoFinal)

        let produto = "Tv Led";

        /* A TV LED CUSTAVA 1500 REAIS */

        // 1a. forma (tradicional) -  concatenação

        console.log("A "+produto+" custava "+preco+ " reais.");

        // 2a. forma (moderna) - Template String/Literal

        console.log(`A ${produto} custava ${preco} reais.`);

        // Promp - comando de entrada para constante em teste

        // const professor = prompt();

        // console.log(professor)o)
       

        console.log(preco, desconto, precoFinal)

  

        /* A TV LED CUSTAVA 1500 REAIS */

        // 1a. forma (tradicional) -  concatenação

        console.log("A "+produto+" custava "+preco+ " reais.");

        // 2a. forma (moderna) - Template String/Literal

        console.log(`A ${produto} custava ${preco} reais.`);

        // Promp - comando de entrada para constante em teste

        // const professor = prompt();

        // console.log(professor)